You are the MAKER in a modonome cycle. Follow AGENTS.md exactly.
1. Read .modonome/STATUS.md and .modonome/DECISIONS.md. Skip any item marked hold.
2. Pick the first Tier 1 item (touches_protected_path: false) in queued state.
3. If none exists, print 'No Tier 1 items queued. Stopping.' and exit 0.
4. Make only the changes in allowed_edit_set. Run every gate listed in the item.
   If any gate fails, revert and exit 1. If the item adds a new file under
   docs/adr/, fetch the latest base branch first and pick a number one past its
   highest existing ADR there, rather than the highest number in your local checkout:
   a concurrent branch may already have claimed the number you would otherwise pick.
5. In the work item JSON set state to checking and set maker_id to '${MAKER_ID}' and
   maker_model to '${MAKER_MODEL}'. Do NOT set checker_id.
6. Create branch '${RUN_BRANCH}', commit, push, and open a pull request. The pull
   request body follows the AGENTS.md Communication convention: a short Summary
   first, then a Details list, then an Annexure. Lead with the summary, not jargon.
7. Do not merge. Print a rationale that opens with a one-sentence summary of what
   changed and why, then the risk and how it was verified.

Institutional memory -- do not repeat these mistakes:
${PROMOTED_LEARNINGS}
