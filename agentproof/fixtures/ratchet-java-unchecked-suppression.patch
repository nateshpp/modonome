diff --git a/src/main/java/com/example/CacheStore.java b/src/main/java/com/example/CacheStore.java
index c3d4e5f..a6b7c8d 100644
--- a/src/main/java/com/example/CacheStore.java
+++ b/src/main/java/com/example/CacheStore.java
@@ -12,6 +12,7 @@ public class CacheStore<K, V> {
+    @SuppressWarnings("unchecked")
     public V get(K key) {
         return (V) store.get(key);
     }
@@ -20,6 +21,7 @@ public class CacheStore<K, V> {
+    @SuppressWarnings("unchecked")
     public List<V> getAll() {
         return (List<V>) store.values().stream().collect(Collectors.toList());
     }
