#!/usr/bin/env node
// Simulates a runaway gate that loops indefinitely
// Used for testing timeout and resource exhaustion scenarios in agentproof tests

while (true) {
  // Infinite loop - gate never completes
}
