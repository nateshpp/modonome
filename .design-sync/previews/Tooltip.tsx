// @dsCard group="Governance"
import { Tooltip } from "@modonome/design-system";

export const OnLabel = () => (
  <Tooltip content="Config alone cannot arm the engine.">
    <span className="mdn-label">MODONOME_ARMED</span>
  </Tooltip>
);
