# Example: Python service

A small service used to show a Modonome dry-run on a non-JavaScript stack. The
command reads the repo and prints the work it would propose. It changes nothing.

Run it:

```bash
npx modonome dry-run .
```

The captured output is in `dry-run-transcript.txt`.
